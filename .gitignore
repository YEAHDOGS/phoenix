# Add offline installers here
Applications/*

# Add drivers here
win-install/drivers

# Local information
*.csv

# Local environment settings
.env

scripts/sys-info/data/
scripts/sys-info/data/*

# Isos
*.iso
qemu/data/*
# Phoenix answer-file generator staging: filled autounattend.xml files carry
# REAL local-account passwords. They must never be committed.
win-install/staging/

# Phoenix toolkit staging (build-time binaries, never commit)
staging/

# Phoenix USB key files carry REAL install-time passwords in reversible form.
# They must never be committed -- the config writers refuse git-tree writes
# without --force for exactly this reason (BOOT-ARCHITECTURE.md §5).
phoenix-config.json
phoenix-config*.json
